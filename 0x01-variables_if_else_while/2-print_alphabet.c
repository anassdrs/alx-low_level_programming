#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print all the alphabet letters
 *
 *
 * Return: Always 0 (sucess)
 **/

int main(void)
{
	char ch = 'a'
		while (ch <= 'z')
		{
			putchar(ch);
			ch++;
		}
	putchar('\n');
	return (0);
}
